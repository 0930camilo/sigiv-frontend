import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sigiv.app',
  appName: 'SIGIV',
  webDir: 'dist/sigiv-web-ui/browser'
};

export default config;